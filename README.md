# NTIX
## Command line tools for NT

These tools provide a posix like experience on the command line, while working entirely with the NT kernel calls.

This is not a posix emulation library, instead maps NT concepts in a posix like way.

# WORK IN PROGRESS

Two test tools so far. ls and mount.

Currently working on robust globbing expansion.
